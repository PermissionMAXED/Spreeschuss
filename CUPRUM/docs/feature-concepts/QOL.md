# Family QOL — Quality of Life, UI & Accessibility (`quality_of_life`)

Global sequence 261–272 · 12 features (12 core / 0 stretch) · core wave W13 · catalog family string: `quality_of_life`

Interface, accessibility and diagnostic features that make a 300-feature mod playable. Per the INDEX retiering note, QOL-11 and QOL-12 are core (genuinely useful team/monitoring utilities are not stretch for arithmetic's sake); the compensating spectacle entries moved to stretch in WEA/TES.

**Security contract (read/write split):** QOL-01/02/04/05/06/07/08/09/12 are **read-only** client conveniences — their only C2S surface is standard GUI actions; QOL-09 shows only networks with at least 1 node owned by the viewer or their team; QOL-10 diagnostics are read-only but **gated** to permission level ≥2 (or `cuprum.diagnostics`). The two **mutating** entries are QOL-03 (wrench: requires owner/team block permission, executes one atomic block-entity-plus-inventory transaction, rolls back if the item cannot be spawned/inserted) and QOL-11 (flare launch: rate-limited to 1 per player per 100 ticks).
**Performance budget:** all QOL HUD widgets combined ≤0.2 ms/frame; sync payloads ≤1 packet/s per widget and ≤1 KiB each; QOL-11 flare payload ≤128 bytes sent once per tracking client plus 1 keep-alive per 5 s; QOL-12 samples at 1 Hz with a 24,000-tick ring buffer (≤32 KiB memory).
**Integration contract (W14):** with REI absent, the mod must boot to a clean dedicated-server tick — REI hooks compile in the client source set only; this no-REI compatibility is verified by the W14 integration gametest `w14_qol07_no_rei_boot`, deliberately outside QOL-07's client-row acceptance.
**Perf-harness note:** the client performance harness referenced by QOL-04 is W14 integration-wave infrastructure (frame-time sampling via the client gametest framework; reference budget: dome + 8 turrets + 64 tube items at ≤16.6 ms/frame on the reference client); QOL-04 lands with its own acceptance assertions and the harness later reuses them — groundwork, not a dangling promise.
**Accessibility invariants (binding):** every color-coded state has a shape/pattern-differentiated variant (QOL-05); every audio cue has a visible counterpart; QOL-04 reduced-effects mode never changes gameplay outcomes, only presentation.
**Cross-family interfaces:** QOL-01 deep-links into U22 handbook chapters; QOL-08 reads the same sync payloads as SHD-12/TOOL-22; QOL-09 renders the PWR graph model; QOL-11 flare recipes consume WEA-06 charge shards.

| ID | Name | Type | Tier | Prog | Wave | Deps | Vanilla overlap | Player behavior | Visual signature | Acceptance | Test |
|---|---|---|---|---|---|---|---|---|---|---|---|
| QOL-01 | Handbook Quick-Access | system | core | 0 | W13 | U22, OXI-03 | none | Keybind plus a "?" button on every mod GUI deep-linking into the exact handbook chapter for the machine at hand. | Book-flip open animation with page-target highlight pulse (T2: flip kept, no pulse; T3: instant open, no animation) | Pressing the keybind opens the handbook, and the "?" button on the OXI-03 kiln GUI opens exactly the kiln chapter (page id equality on 3 sampled GUIs). | client_gametest:qol01_quickopen_links |
| QOL-02 | Machine GUI Standard | system | core | 0 | W13 | U05 | none | Unified GUI layout for all machines: charge bar, IO side configurator, upgrade slots and status lamp always in the same places. | Consistent copper-frame GUI skin with standard status lamp colors (T2: same skin; T3: flat untextured frame, same layout) | Each of 5 sampled machine GUIs exposes the charge-bar widget, IO tab and status lamp at the standard layout ids (widget presence and position assertions). | client_gametest:qol02_gui_standard |
| QOL-03 | Wrench | item | core | 0 | W13 | - | adjacent: no vanilla rotate/pickup tool | The mod's one adjustment tool: rotate machines, toggle IO faces, and sneak-click to pick up machines with inventory and settings intact — owner/team-permitted only, executed as one atomic transaction. | Wrench turn animation with click detents and ratchet sounds (T2: same animation; T3: no animation, ratchets kept) | Sneak-wrenching by the owner drops exactly 1 item that, re-placed, restores inventory, IO config and stored charge byte-identically (NBT equality across all 3 categories); a non-owner attempt on a claimed machine changes 0 blocks; if the drop cannot spawn or insert, the machine remains in place with state intact (rollback, 0 loss). | server_gametest:qol03_wrench_preserves |
| QOL-04 | Reduced Effects Mode | system | core | 0 | W13 | - | none | One config toggle swapping every T1 shader effect to its T2/T3 fallback and capping particle counts — playability on weak hardware and for photosensitive players. | Side-by-side identical layouts with simplified visuals — this feature is the fallback switch itself (T2: it selects T2 assets; T3: it selects T3 assets) | With the toggle on, the T1 pipeline-use probe counts 0 custom pipeline submissions in a scene containing dome, arcs and auroras, and a dome-intercept gametest passes identically in both modes. | client_gametest:qol04_reduced_effects |
| QOL-05 | Colorblind Indicator Set | system | core | 0 | W13 | - | none | Alternative indicator skin replacing color-only states with shapes and patterns (charge = diagonal fill, fault = cross-hatch) across HUD, GUIs and block lamps. | Shape-glyph overlays on every stateful indicator (T2: same glyphs; T3: glyphs plus text letters for the 4 core states) | The asset-table audit lists 0 stateful indicators lacking a shape-differentiated variant, and the colorblind mapping table maps all 3 sampled indicator ids to distinct shape glyph ids (data assertions only). | unit_test:qol05_indicator_audit |
| QOL-06 | Audio Mixer Categories | system | core | 0 | W13 | - | extends: vanilla sound sliders are coarse categories | Dedicated volume sliders for mod sound groups (machines, arcs, ambience, alarms) with alarm-priority ducking. | Mixer panel GUI with live VU-style preview meters (T2: static meter graphics; T3: plain sliders, no meters) | Setting the machines slider to 0 silences machine loop instances within 20 ticks while alarm sounds still play at full volume (sound-engine instance assertions). | client_gametest:qol06_mixer_silences |
| QOL-07 | REI Integration | integration | core | 0 | W13 | - | none | Full recipe-viewer support: kiln alloys, refinery yields, casting molds and charge costs browsable in REI. | Custom REI category widgets with charge-cost badges (T2: same widgets; T3: default REI widget style, badges as text) | With REI 21.9.813 loaded, all 4 machine categories appear with their full recipe lists (count equality against the recipe manager, 4 of 4 categories present). | client_gametest:qol07_rei_categories |
| QOL-08 | Charge HUD Widget | system | core | 1 | W13 | U05 | none | Optional corner HUD showing carried battery charge, nearest-network status and active module drain — the at-a-glance power dashboard. | Compact copper gauge cluster (T2: flat gauge graphics; T3: plain text lines) | The widget's three readouts equal the server-side values within 250 ms across 3 scripted state changes, and the widget hides when no chargeable item is carried. | client_gametest:qol08_hud_syncs |
| QOL-09 | Network Overview Map | system | core | 2 | W13 | U05 | none | Handbook-embedded live diagram of your charge networks: nodes, flows, faults — SimCity for your copper grid, scoped to networks you own or share via team. | Node-graph render with animated flow dashes and blinking fault nodes (T2: static edges, fault color kept; T3: text node list) | The rendered graph's node and edge sets equal the server model for a 12-node network owned by the viewer; a foreign player's network contributes 0 nodes; a scripted fault marks exactly that node. | client_gametest:qol09_graph_view |
| QOL-10 | Diagnostics Overlay | system | core | 2 | W13 | - | extends: F3 exists; this is mod-scoped | OP-gated read-only debug feed listing per-system tick costs, entity counts and network stats — the server owner's tuning lens. | Translucent diagnostics panel with per-family bar meters (T2: flat panel; T3: text-only table) | Diagnostics payloads are sent only to permission level ≥2 (or `cuprum.diagnostics`); a non-OP request is refused with exactly 1 denial message and 0 payloads; payload per-system sections match the server profiler sections 1:1; the endpoint performs 0 state mutations. | server_gametest:qol10_diag_gated |
| QOL-11 | Copper Flare | item | core | 1 | W13 | WEA-06 | adjacent: firework stars are uncontrolled; this is a team ping | Team signal flare shooting a colored, named beam column visible to teammates at long range — cheap coordination (crafted from 2 charge shards; launches rate-limited to 1 per player per 100 ticks). | Vertical light column with team color and floating label (T1 beam shader) (T2: particle column with label; T3: label billboard only) | The flare column syncs to all team members' clients within 200 blocks (tracking-range payload ≥208 blocks asserted; spawn payload ≤128 bytes), persists exactly 1,200 ticks, and is invisible to non-team players. | client_gametest:qol11_flare_team_sync |
| QOL-12 | Statistics Dashboard | block | core | 2 | W13 | U05 | none | Wall-mounted base dashboard cycling live graphs of charge generation, storage and consumption over the last game day — monitoring as furniture (an optional GEN-05 lens variant adds a magnified gauge face). | Line-graph screen with sweeping redraw animation and soft CRT hum (T2: static line graph refreshed per sample; T3: 3-bar static chart, no hum) | The dashboard samples at 1 Hz and a scripted +100 Cg/t generation step appears in the client-rendered graph within 1 sample (20 ticks); stored history covers exactly the last 24,000 ticks. | client_gametest:qol12_dashboard_graph |
